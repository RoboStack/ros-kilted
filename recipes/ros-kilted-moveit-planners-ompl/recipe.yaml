# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-kilted-moveit-planners-ompl
  version: 2.13.2
source:
  git: https://github.com/ros2-gbp/moveit2-release.git
  tag: release/kilted/moveit_planners_ompl/2.13.2-2
  target_directory: ros-kilted-moveit-planners-ompl/src/work
  patches:
    - patch/ros-kilted-moveit-planners-ompl.patch

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 7
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - eigen
    - libgomp
    - numpy
    - pip
    - python
    - ros-kilted-ament-cmake
    - ros-kilted-ament-cmake-gtest
    - ros-kilted-eigen3-cmake-module
    - ros-kilted-moveit-common
    - ros-kilted-moveit-core
    - ros-kilted-moveit-msgs
    - ros-kilted-moveit-resources-fanuc-moveit-config
    - ros-kilted-moveit-resources-panda-moveit-config
    - ros-kilted-moveit-resources-pr2-description
    - ros-kilted-moveit-ros-planning
    - ros-kilted-ompl
    - ros-kilted-pluginlib
    - ros-kilted-rclcpp
    - ros-kilted-ros-environment
    - ros-kilted-ros-workspace
    - ros-kilted-tf2-eigen
    - ros-kilted-tf2-ros
    - ros2-distro-mutex 0.9.* kilted_*
  run:
    - libgomp
    - python
    - ros-kilted-moveit-common
    - ros-kilted-moveit-core
    - ros-kilted-moveit-msgs
    - ros-kilted-moveit-ros-planning
    - ros-kilted-ompl
    - ros-kilted-pluginlib
    - ros-kilted-rclcpp
    - ros-kilted-ros-workspace
    - ros-kilted-tf2-eigen
    - ros-kilted-tf2-ros
    - ros2-distro-mutex 0.9.* kilted_*
    - if: osx and x86_64
      then:
        - __osx >=${{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}

# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-kilted-moveit-servo
  version: 2.14.1
source:
  git: https://github.com/ros2-gbp/moveit2-release.git
  tag: release/kilted/moveit_servo/2.14.1-1
  target_directory: ros-kilted-moveit-servo/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 15
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://moveit.github.io/moveit_tutorials
  license: BSD-3-Clause
  summary: Provides real-time manipulator Cartesian and joint servoing.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - python
    - ros-kilted-ament-cmake
    - ros-kilted-ament-cmake-gtest
    - ros-kilted-control-msgs
    - ros-kilted-generate-parameter-library
    - ros-kilted-geometry-msgs
    - ros-kilted-moveit-common
    - ros-kilted-moveit-core
    - ros-kilted-moveit-msgs
    - ros-kilted-moveit-resources-panda-moveit-config
    - ros-kilted-moveit-ros-planning
    - ros-kilted-moveit-ros-planning-interface
    - ros-kilted-pluginlib
    - ros-kilted-realtime-tools
    - ros-kilted-ros-environment
    - ros-kilted-ros-testing
    - ros-kilted-ros-workspace
    - ros-kilted-sensor-msgs
    - ros-kilted-std-msgs
    - ros-kilted-std-srvs
    - ros-kilted-tf2-eigen
    - ros-kilted-trajectory-msgs
    - ros2-distro-mutex 0.13.* kilted_*
  run:
    - python
    - ros-kilted-control-msgs
    - ros-kilted-controller-manager
    - ros-kilted-generate-parameter-library
    - ros-kilted-geometry-msgs
    - ros-kilted-joint-state-broadcaster
    - ros-kilted-joint-trajectory-controller
    - ros-kilted-joy
    - ros-kilted-launch-param-builder
    - ros-kilted-moveit-common
    - ros-kilted-moveit-configs-utils
    - ros-kilted-moveit-core
    - ros-kilted-moveit-msgs
    - ros-kilted-moveit-ros-planning
    - ros-kilted-moveit-ros-planning-interface
    - ros-kilted-moveit-ros-visualization
    - ros-kilted-pluginlib
    - ros-kilted-realtime-tools
    - ros-kilted-robot-state-publisher
    - ros-kilted-ros-workspace
    - ros-kilted-sensor-msgs
    - ros-kilted-std-msgs
    - ros-kilted-std-srvs
    - ros-kilted-tf2-eigen
    - ros-kilted-tf2-ros
    - ros-kilted-trajectory-msgs
    - ros2-distro-mutex 0.13.* kilted_*
    - if: osx and x86_64
      then:
        - __osx >=${{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}

# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-kilted-dual-laser-merger
  version: 0.0.1
source:
  git: https://github.com/ros2-gbp/dual_laser_merger-release.git
  tag: release/kilted/dual_laser_merger/0.0.1-2
  target_directory: ros-kilted-dual-laser-merger/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 15
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  license: Apache-2.0
  summary: merge dual lidar's scans.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - libboost-devel
    - numpy
    - pcl
    - pip
    - python
    - ros-kilted-ament-cmake
    - ros-kilted-ament-cmake-auto
    - ros-kilted-ament-copyright
    - ros-kilted-ament-cpplint
    - ros-kilted-ament-flake8
    - ros-kilted-ament-lint-auto
    - ros-kilted-ament-lint-common
    - ros-kilted-ament-pep257
    - ros-kilted-ament-xmllint
    - ros-kilted-geometry-msgs
    - ros-kilted-laser-geometry
    - ros-kilted-message-filters
    - ros-kilted-pcl-conversions
    - ros-kilted-pcl-ros
    - ros-kilted-rclcpp
    - ros-kilted-rclcpp-components
    - ros-kilted-ros-environment
    - ros-kilted-ros-workspace
    - ros-kilted-tf2
    - ros-kilted-tf2-ros
    - ros-kilted-tf2-sensor-msgs
    - ros2-distro-mutex 0.13.* kilted_*
    - vtk-base
  run:
    - libboost-devel
    - pcl
    - python
    - ros-kilted-geometry-msgs
    - ros-kilted-laser-geometry
    - ros-kilted-message-filters
    - ros-kilted-pcl-conversions
    - ros-kilted-pcl-ros
    - ros-kilted-rclcpp
    - ros-kilted-rclcpp-components
    - ros-kilted-ros-workspace
    - ros-kilted-tf2
    - ros-kilted-tf2-ros
    - ros-kilted-tf2-sensor-msgs
    - ros2-distro-mutex 0.13.* kilted_*
    - vtk-base
    - if: osx and x86_64
      then:
        - __osx >=${{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}

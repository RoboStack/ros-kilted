# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-kilted-rviz-default-plugins
  version: 15.0.11
source:
  git: https://github.com/ros2-gbp/rviz-release.git
  tag: release/kilted/rviz_default_plugins/15.0.11-1
  target_directory: ros-kilted-rviz-default-plugins/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 15
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://github.com/ros2/rviz/blob/ros2/README.md
  repository: https://github.com/ros2/rviz
  license: BSD-3-Clause
  summary: Several default plugins for rviz to cover the basic functionality.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: build_platform != target_platform
      then:
        - qt-main
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - python
    - qt-main
    - ros-kilted-ament-cmake-gmock
    - ros-kilted-ament-cmake-gtest
    - ros-kilted-ament-cmake-lint-cmake
    - ros-kilted-ament-cmake-ros
    - ros-kilted-ament-index-cpp
    - ros-kilted-ament-lint-auto
    - ros-kilted-ament-lint-common
    - ros-kilted-geometry-msgs
    - ros-kilted-gz-math-vendor
    - ros-kilted-image-transport
    - ros-kilted-interactive-markers
    - ros-kilted-laser-geometry
    - ros-kilted-map-msgs
    - ros-kilted-nav-msgs
    - ros-kilted-pluginlib
    - ros-kilted-point-cloud-transport
    - ros-kilted-rclcpp
    - ros-kilted-resource-retriever
    - ros-kilted-ros-environment
    - ros-kilted-ros-workspace
    - ros-kilted-rviz-common
    - ros-kilted-rviz-ogre-vendor
    - ros-kilted-rviz-rendering
    - ros-kilted-rviz-rendering-tests
    - ros-kilted-rviz-resource-interfaces
    - ros-kilted-rviz-visual-testing-framework
    - ros-kilted-tf2
    - ros-kilted-tf2-geometry-msgs
    - ros-kilted-tf2-ros
    - ros-kilted-urdf
    - ros-kilted-visualization-msgs
    - ros2-distro-mutex 0.13.* kilted_*
  run:
    - python
    - qt-main
    - ros-kilted-geometry-msgs
    - ros-kilted-gz-math-vendor
    - ros-kilted-image-transport
    - ros-kilted-interactive-markers
    - ros-kilted-laser-geometry
    - ros-kilted-map-msgs
    - ros-kilted-nav-msgs
    - ros-kilted-pluginlib
    - ros-kilted-point-cloud-transport
    - ros-kilted-rclcpp
    - ros-kilted-resource-retriever
    - ros-kilted-ros-workspace
    - ros-kilted-rviz-common
    - ros-kilted-rviz-ogre-vendor
    - ros-kilted-rviz-rendering
    - ros-kilted-rviz-resource-interfaces
    - ros-kilted-tf2
    - ros-kilted-tf2-geometry-msgs
    - ros-kilted-tf2-ros
    - ros-kilted-urdf
    - ros-kilted-visualization-msgs
    - ros2-distro-mutex 0.13.* kilted_*
    - if: osx and x86_64
      then:
        - __osx >=${{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}

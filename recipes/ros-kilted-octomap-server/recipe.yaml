# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-kilted-octomap-server
  version: 2.3.0
source:
  git: https://github.com/ros2-gbp/octomap_mapping-release.git
  tag: release/kilted/octomap_server/2.3.0-2
  target_directory: ros-kilted-octomap-server/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 15
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: http://www.ros.org/wiki/octomap_server
  repository: https://github.com/OctoMap/octomap_mapping
  license: BSD-3-Clause
  summary: octomap_server loads a 3D map (as Octree-based OctoMap) and distributes it to other nodes in a compact binary format. It also allows to incrementally build 3D OctoMaps, and provides map saving in the node octomap_saver.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - libboost-devel
    - libgl-devel
    - libopengl-devel
    - numpy
    - pcl
    - pip
    - python
    - ros-kilted-ament-cmake-auto
    - ros-kilted-ament-lint-auto
    - ros-kilted-ament-lint-common
    - ros-kilted-geometry-msgs
    - ros-kilted-message-filters
    - ros-kilted-nav-msgs
    - ros-kilted-octomap-msgs
    - ros-kilted-octomap-ros
    - ros-kilted-pcl-conversions
    - ros-kilted-pcl-ros
    - ros-kilted-rclcpp
    - ros-kilted-rclcpp-components
    - ros-kilted-ros-environment
    - ros-kilted-ros-workspace
    - ros-kilted-sensor-msgs
    - ros-kilted-std-msgs
    - ros-kilted-std-srvs
    - ros-kilted-tf2
    - ros-kilted-tf2-eigen
    - ros-kilted-tf2-geometry-msgs
    - ros-kilted-tf2-ros
    - ros-kilted-visualization-msgs
    - ros2-distro-mutex 0.13.* kilted_*
    - vtk-base
  run:
    - libboost-devel
    - libgl-devel
    - libopengl-devel
    - pcl
    - python
    - ros-kilted-geometry-msgs
    - ros-kilted-message-filters
    - ros-kilted-nav-msgs
    - ros-kilted-octomap-msgs
    - ros-kilted-octomap-ros
    - ros-kilted-pcl-conversions
    - ros-kilted-pcl-ros
    - ros-kilted-rclcpp
    - ros-kilted-rclcpp-components
    - ros-kilted-ros-workspace
    - ros-kilted-sensor-msgs
    - ros-kilted-std-msgs
    - ros-kilted-std-srvs
    - ros-kilted-tf2
    - ros-kilted-tf2-eigen
    - ros-kilted-tf2-geometry-msgs
    - ros-kilted-tf2-ros
    - ros-kilted-visualization-msgs
    - ros2-distro-mutex 0.13.* kilted_*
    - vtk-base
    - if: osx and x86_64
      then:
        - __osx >=${{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}

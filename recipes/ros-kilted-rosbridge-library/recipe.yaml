# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-kilted-rosbridge-library
  version: 3.0.2
source:
  git: https://github.com/ros2-gbp/rosbridge_suite-release.git
  tag: release/kilted/rosbridge_library/3.0.2-1
  target_directory: ros-kilted-rosbridge-library/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 15
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: http://ros.org/wiki/rosbridge_library
  repository: https://github.com/RobotWebTools/rosbridge_suite
  license: BSD-3-Clause
  summary: The core rosbridge package, responsible for interpreting JSON and performing the appropriate ROS action, like subscribe, publish, call service, and interact with params.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - python
    - ros-kilted-action-msgs
    - ros-kilted-ament-cmake-mypy
    - ros-kilted-ament-cmake-python
    - ros-kilted-ament-cmake-ros
    - ros-kilted-builtin-interfaces
    - ros-kilted-control-msgs
    - ros-kilted-diagnostic-msgs
    - ros-kilted-example-interfaces
    - ros-kilted-geometry-msgs
    - ros-kilted-nav-msgs
    - ros-kilted-ros-environment
    - ros-kilted-ros-workspace
    - ros-kilted-rosbridge-test-msgs
    - ros-kilted-sensor-msgs
    - ros-kilted-std-msgs
    - ros-kilted-std-srvs
    - ros-kilted-stereo-msgs
    - ros-kilted-tf2-msgs
    - ros-kilted-trajectory-msgs
    - ros-kilted-visualization-msgs
    - ros2-distro-mutex 0.13.* kilted_*
  run:
    - numpy
    - pillow
    - pymongo
    - python
    - ros-kilted-rcl-interfaces
    - ros-kilted-rclpy
    - ros-kilted-ros-workspace
    - ros2-distro-mutex 0.13.* kilted_*
    - if: osx and x86_64
      then:
        - __osx >=${{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}

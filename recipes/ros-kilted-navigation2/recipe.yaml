# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-kilted-navigation2
  version: 1.4.2
source:
  git: https://github.com/ros2-gbp/navigation2-release.git
  tag: release/kilted/navigation2/1.4.2-1
  target_directory: ros-kilted-navigation2/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 15
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  license: Apache-2.0
  summary: ROS2 Navigation Stack
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - python
    - ros-kilted-ament-cmake
    - ros-kilted-nav2-common
    - ros-kilted-ros-environment
    - ros-kilted-ros-workspace
    - ros2-distro-mutex 0.13.* kilted_*
  run:
    - python
    - ros-kilted-nav2-amcl
    - ros-kilted-nav2-behavior-tree
    - ros-kilted-nav2-behaviors
    - ros-kilted-nav2-bt-navigator
    - ros-kilted-nav2-collision-monitor
    - ros-kilted-nav2-constrained-smoother
    - ros-kilted-nav2-controller
    - ros-kilted-nav2-core
    - ros-kilted-nav2-costmap-2d
    - ros-kilted-nav2-dwb-controller
    - ros-kilted-nav2-graceful-controller
    - ros-kilted-nav2-lifecycle-manager
    - ros-kilted-nav2-map-server
    - ros-kilted-nav2-mppi-controller
    - ros-kilted-nav2-msgs
    - ros-kilted-nav2-navfn-planner
    - ros-kilted-nav2-planner
    - ros-kilted-nav2-regulated-pure-pursuit-controller
    - ros-kilted-nav2-rotation-shim-controller
    - ros-kilted-nav2-route
    - ros-kilted-nav2-rviz-plugins
    - ros-kilted-nav2-simple-commander
    - ros-kilted-nav2-smac-planner
    - ros-kilted-nav2-smoother
    - ros-kilted-nav2-theta-star-planner
    - ros-kilted-nav2-util
    - ros-kilted-nav2-velocity-smoother
    - ros-kilted-nav2-voxel-grid
    - ros-kilted-nav2-waypoint-follower
    - ros-kilted-opennav-docking
    - ros-kilted-opennav-docking-bt
    - ros-kilted-opennav-docking-core
    - ros-kilted-ros-workspace
    - ros2-distro-mutex 0.13.* kilted_*
    - if: osx and x86_64
      then:
        - __osx >=${{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}

# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-kilted-controller-interface
  version: 5.1.0
source:
  git: https://github.com/ros2-gbp/ros2_control-release.git
  tag: release/kilted/controller_interface/5.1.0-1
  target_directory: ros-kilted-controller-interface/src/work
  patches:
    - patch/ros-kilted-controller-interface.osx.patch

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 7
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - fmt
    - numpy
    - pip
    - python
    - ros-kilted-ament-cmake
    - ros-kilted-ament-cmake-gen-version-h
    - ros-kilted-ament-cmake-gmock
    - ros-kilted-geometry-msgs
    - ros-kilted-hardware-interface
    - ros-kilted-rclcpp-lifecycle
    - ros-kilted-realtime-tools
    - ros-kilted-ros-environment
    - ros-kilted-ros-workspace
    - ros-kilted-ros2-control-cmake
    - ros-kilted-sensor-msgs
    - ros2-distro-mutex 0.9.* kilted_*
  run:
    - python
    - ros-kilted-hardware-interface
    - ros-kilted-rclcpp-lifecycle
    - ros-kilted-realtime-tools
    - ros-kilted-ros-workspace
    - ros2-distro-mutex 0.9.* kilted_*
    - if: osx and x86_64
      then:
        - __osx >=${{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}

# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-kilted-geometric-shapes
  version: 2.3.3
source:
  git: https://github.com/ros2-gbp/geometric_shapes-release.git
  tag: release/kilted/geometric_shapes/2.3.3-1
  target_directory: ros-kilted-geometric-shapes/src/work
  patches:
    - patch/ros-kilted-geometric-shapes.patch

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 7
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: target_platform == 'emscripten-wasm32'
      then:
        - ros-kilted-rosidl-default-generators
  host:
    - assimp
    - if: build_platform == target_platform
      then:
        - pkg-config
    - eigen
    - fcl
    - libboost-devel
    - numpy
    - octomap
    - pip
    - pkg-config
    - python
    - qhull
    - ros-kilted-ament-cmake
    - ros-kilted-ament-cmake-copyright
    - ros-kilted-ament-cmake-gtest
    - ros-kilted-ament-lint-auto
    - ros-kilted-ament-lint-cmake
    - ros-kilted-console-bridge-vendor
    - ros-kilted-eigen-stl-containers
    - ros-kilted-eigen3-cmake-module
    - ros-kilted-geometry-msgs
    - ros-kilted-random-numbers
    - ros-kilted-rclcpp
    - ros-kilted-resource-retriever
    - ros-kilted-ros-environment
    - ros-kilted-ros-workspace
    - ros-kilted-rosidl-default-generators
    - ros-kilted-shape-msgs
    - ros-kilted-visualization-msgs
    - ros2-distro-mutex 0.9.* kilted_*
  run:
    - assimp
    - eigen
    - libboost
    - libboost-devel
    - python
    - qhull
    - ros-kilted-console-bridge-vendor
    - ros-kilted-eigen-stl-containers
    - ros-kilted-eigen3-cmake-module
    - ros-kilted-geometry-msgs
    - ros-kilted-random-numbers
    - ros-kilted-rclcpp
    - ros-kilted-resource-retriever
    - ros-kilted-ros-workspace
    - ros-kilted-rosidl-default-runtime
    - ros-kilted-shape-msgs
    - ros-kilted-visualization-msgs
    - ros2-distro-mutex 0.9.* kilted_*
    - if: osx and x86_64
      then:
        - __osx >=${{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
